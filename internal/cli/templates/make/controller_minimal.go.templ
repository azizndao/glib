package {{.PkgName}}

import (
	"context"
	
	"github.com/azizndao/glib"
)

// @Controller path={{.RoutePrefix}}
type Controller struct {
	// Add dependencies here (auto-injected by type from providers)
}

// @Route method=GET path=/
func (c *Controller) Index(ctx context.Context) glib.Result[any] {
	// TODO: implement
	return glib.OK[any](nil)
}
