// Code generated by glib. DO NOT EDIT.
package {{ .PackageName }}

import (
	"fmt"
	"os"
	"strconv"
{{- if .NeedsTime }}
	"time"
{{- end }}
{{- if .NeedsURL }}
	"net/url"
{{- end }}
	
	"{{ .ConfigImportPath }}"
)

// loadConfig loads configuration from environment variables.
// It returns an error if required fields are missing or invalid.
// This is a private function called internally by the DI container.
func loadConfig() (*{{ .ConfigPackageName }}.Config, error) {
	cfg := &{{ .ConfigPackageName }}.Config{}
	
{{- range .Fields }}
	{{ template "parseField" . }}
{{- end }}

	// Validation
	var validationErrors []string
{{- range .Fields }}
	{{ template "validateField" . }}
{{- end }}
	
	if len(validationErrors) > 0 {
		return nil, fmt.Errorf("config validation failed:\n  - %s", 
			validationErrors[0])
	}

	return cfg, nil
}

{{ define "parseField" }}
{{- if .IsNested }}
	// Parse nested struct: {{ .Name }}
{{- range .Fields }}
	{{ template "parseField" . }}
{{- end }}
{{- else }}
	// {{ .EnvName }}{{ if .Required }} (required){{ end }}{{ if .DefaultValue }} [default: {{ .DefaultValue }}]{{ end }}
	{{- if eq .Type.Name "string" }}
	cfg.{{ .ParentPath }}{{ .Name }} = getEnvHelper("{{ .EnvName }}", "{{ .DefaultValue }}")
	{{- else if eq .Type.Name "int" }}
	if v := os.Getenv("{{ .EnvName }}"); v != "" {
		val, err := strconv.Atoi(v)
		if err != nil {
			return nil, fmt.Errorf("{{ .EnvName }}: invalid integer: %w", err)
		}
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ if .DefaultValue }} else {
		cfg.{{ .ParentPath }}{{ .Name }} = {{ .DefaultValue }}
	}{{ end }}
	{{- else if eq .Type.Name "int64" }}
	if v := os.Getenv("{{ .EnvName }}"); v != "" {
		val, err := strconv.ParseInt(v, 10, 64)
		if err != nil {
			return nil, fmt.Errorf("{{ .EnvName }}: invalid int64: %w", err)
		}
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ if .DefaultValue }} else {
		cfg.{{ .ParentPath }}{{ .Name }} = {{ .DefaultValue }}
	}{{ end }}
	{{- else if eq .Type.Name "bool" }}
	if v := os.Getenv("{{ .EnvName }}"); v != "" {
		val, err := strconv.ParseBool(v)
		if err != nil {
			return nil, fmt.Errorf("{{ .EnvName }}: invalid boolean: %w", err)
		}
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ if .DefaultValue }} else {
		cfg.{{ .ParentPath }}{{ .Name }} = {{ .DefaultValue }}
	}{{ end }}
	{{- else if eq .Type.Name "float64" }}
	if v := os.Getenv("{{ .EnvName }}"); v != "" {
		val, err := strconv.ParseFloat(v, 64)
		if err != nil {
			return nil, fmt.Errorf("{{ .EnvName }}: invalid float: %w", err)
		}
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ if .DefaultValue }} else {
		cfg.{{ .ParentPath }}{{ .Name }} = {{ .DefaultValue }}
	}{{ end }}
	{{- else if eq .Type.FullName "time.Duration" }}
	if v := os.Getenv("{{ .EnvName }}"); v != "" {
		val, err := time.ParseDuration(v)
		if err != nil {
			return nil, fmt.Errorf("{{ .EnvName }}: invalid duration: %w", err)
		}
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ if .DefaultValue }} else {
		val, _ := time.ParseDuration("{{ .DefaultValue }}")
		cfg.{{ .ParentPath }}{{ .Name }} = val
	}{{ end }}
	{{- else }}
	// Unsupported type: {{ .Type.FullName }}
	cfg.{{ .ParentPath }}{{ .Name }} = os.Getenv("{{ .EnvName }}")
	{{- end }}
{{- end }}
{{ end }}

{{ define "validateField" }}
{{- if not .IsNested }}
{{- if .Required }}
	if cfg.{{ .ParentPath }}{{ .Name }} == {{ if eq .Type.Name "string" }}""{{ else }}0{{ end }} {
		validationErrors = append(validationErrors, "{{ .EnvName }} is required")
	}
{{- end }}
{{- else }}
{{- range .Fields }}
	{{ template "validateField" . }}
{{- end }}
{{- end }}
{{ end }}

// getEnvHelper retrieves an environment variable or returns the fallback
func getEnvHelper(key, fallback string) string {
	if value := os.Getenv(key); value != "" {
		return value
	}
	return fallback
}
